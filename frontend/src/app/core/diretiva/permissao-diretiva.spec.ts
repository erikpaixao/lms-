import { PermissaoDiretiva } from './permissao-diretiva';

describe('PermissaoDiretiva', () => {
  it('should create an instance', () => {
    const directive = new PermissaoDiretiva();
    expect(directive).toBeTruthy();
  });
});
